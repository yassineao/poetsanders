import { mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const projectRoot = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const outputPath = resolve(
  projectRoot,
  'src/environments/environment.generated.ts',
);
const apiBaseUrl = (
  process.env.API_BASE_URL ?? 'https://autoanders-api.onrender.com'
).replace(/\/+$/, '');
const dealershipUrl = (
  process.env.DEALERSHIP_URL ?? 'https://auto-anders.vercel.app'
).replace(/\/+$/, '');

const output = `// Generated by scripts/generate-environment.mjs.
export const environment = {
  apiBaseUrl: ${JSON.stringify(apiBaseUrl)},
  dealershipUrl: ${JSON.stringify(dealershipUrl)},
} as const;
`;

mkdirSync(dirname(outputPath), { recursive: true });
writeFileSync(outputPath, output, 'utf8');
